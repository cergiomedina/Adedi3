<%= form_for(@detalle_arriendo) do |f| %>
  <% if @detalle_arriendo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detalle_arriendo.errors.count, "error") %> prohibited this detalle_arriendo from being saved:</h2>

      <ul>
      <% @detalle_arriendo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
