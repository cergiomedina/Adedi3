<%= form_for(@transicion_est_ejemplar) do |f| %>
  <% if @transicion_est_ejemplar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transicion_est_ejemplar.errors.count, "error") %> prohibited this transicion_est_ejemplar from being saved:</h2>

      <ul>
      <% @transicion_est_ejemplar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
