<%= form_for(@transicion_arriendo) do |f| %>
  <% if @transicion_arriendo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transicion_arriendo.errors.count, "error") %> prohibited this transicion_arriendo from being saved:</h2>

      <ul>
      <% @transicion_arriendo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
