<%= form_for(@arriendo_estado) do |f| %>
  <% if @arriendo_estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arriendo_estado.errors.count, "error") %> prohibited this arriendo_estado from being saved:</h2>

      <ul>
      <% @arriendo_estado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
