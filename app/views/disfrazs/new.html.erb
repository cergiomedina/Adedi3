<div class="row">
	<div class="container row red-text">
		<% if @disfraz.errors.any? %>
    <div id="error_explanation">
      <h2>Errores encontrados:</h2>

      <ul>
      <% @disfraz.errors.each do |attribute, message| %>
        <% if message.is_a?(String)%>
            <li><%= message %></li>
        <% end %>
      <% end %>
      </ul>
    </div>
  <% end %>

	</div>
	<div class="col s12 m3">

		<div class="card-panel">
			<h5>Crear Categoría</h5>
			<div class="divider"></div>
			<p>
			<%= link_to "Nueva Categoría", "/categoria/new",class: 'waves-effect waves-light btn'%>
			</p>
		</div>
		
		<div class="card-panel blue darken-1 white-text">
			<h5>Añadir Disfraz</h5>
			<div class="divider"></div>
			<p>Acá puedes añadir los disfraces nuevos a la tienda. Ten en cuenta que los disfraces tendrán automáticamente el stock disponible que aquí señales.</p>
		</div>
		
	</div>
	<div class="col s12 m6">

		<% if Categoria.all.count == 0%>

			<div class="card-panel">
				<h1>No tienes categorías en la aplicación.</h1>
				<div class="divider"></div>
				<p>Añade una categoría en el menú de la izquierda</p>
				
			</div>
		<%else%>
			<div class="card-panel">
				<h1>Añadir disfraz</h1>
				<div class="divider"></div>
				<%= render 'form' %>

			</div>
		<%end%>

	</div>

	<div class="col s12 m3">
	
	</div>

</div>