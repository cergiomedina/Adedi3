<h1>Listing transicion_est_pedidos</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @transicion_est_pedidos.each do |transicion_est_pedido| %>
      <tr>
        <td><%= link_to 'Show', transicion_est_pedido %></td>
        <td><%= link_to 'Edit', edit_transicion_est_pedido_path(transicion_est_pedido) %></td>
        <td><%= link_to 'Destroy', transicion_est_pedido, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Transicion est pedido', new_transicion_est_pedido_path %>
