<%= form_for(@transicion_est_pedido) do |f| %>
  <% if @transicion_est_pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transicion_est_pedido.errors.count, "error") %> prohibited this transicion_est_pedido from being saved:</h2>

      <ul>
      <% @transicion_est_pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
