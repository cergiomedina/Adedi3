<%= form_for(@cliente_estado) do |f| %>
  <% if @cliente_estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente_estado.errors.count, "error") %> prohibited this cliente_estado from being saved:</h2>

      <ul>
      <% @cliente_estado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
