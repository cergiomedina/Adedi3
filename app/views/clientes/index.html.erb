<div class="row">
    <div class="col s12"><p></p></div>
    <div class="col s12 m4 l3">

      <div class="card-panel green darken-3 white-text">

          <h4><i class="fa fa-hand-o-right"></i> Información</h4>
          <p>En esta sección se muestran los clientes que están registrados en la aplicación. Desde acá puedes modificar su información personal o eliminarlos, si así lo deseas.</p>
        </div>

    </div>
    <div class="col s12 m4 l9"><p> </p>
      <div class="card-panel" >
          <h2><i class="fa fa-user"></i> Clientes </h2>

          <table>
            <thead>
              <tr>
                <th>Id</th>
                <th>Rut</th>
                <th>Nombre</th>
                <th>Telefono</th>
                <th>Direccion</th>
                <th>Email</th>

              </tr>
            </thead>

            <tbody>
              <% @clientes.each do |cliente| %>
                <tr>
                  <td><%= cliente.id %></td>
                  <td><%= cliente.RUT_CLIENTE%></td>
                  <td><%= cliente.NOMBRE_CLIENTE%> <%= cliente.APELLIDO_CLIENTE%></td>
                  <td><%= cliente.TELEFONO_CLIENTE%></td>
                  <td><%= cliente.DIRECCION_CLIENTE%></td>
                  <td><%= cliente.email%></td>
                  <td>
                  <%= link_to 'Modificar', edit_cliente_path(cliente) %>
                  <%= link_to 'Eliminar', cliente, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
              
                </tr>
           
                  

              <% end %>
            </tbody>
          </table>

          <br>

  </div>

    </div>
    </div>
    </div>
    </div>

<!--<%= link_to 'New Cliente', new_cliente_path %>

-->
